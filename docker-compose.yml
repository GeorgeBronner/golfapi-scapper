version: '3.8'

services:
  scraper:
    build: .
    container_name: golf-scraper
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
